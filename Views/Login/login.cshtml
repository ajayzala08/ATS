@model ATS2019_2.Models.loginmodel
@{
    ViewBag.Title = "login";
    Layout = "~/Views/Login/_LoginPageMaster.cshtml";
}

@using (Html.BeginForm("checklogin", "Login", FormMethod.Post))
{

<div class="log">
    <div class="container">
        <div class='wrap'>
            <div class="logo">
                <img src="~/img/arche2017-1k.png">
            </div>
            <h2>Login</h2>

            <form>
                @Html.TextBoxFor(m => m.username, new { @id = "username", @placeholder = "username", @required = "required" })

                @*<input type='text' id='username' placeholder='Username'>*@
                @Html.PasswordFor(m => m.pwd, new { @id = "password", @placeholder = "Password", @value = Model.pwd, @required = "required" })

                @*<input type='password' id='password' placeholder='Password'>*@
                @Html.CheckBoxFor(m => m.rememberme, new { @class = "check" })
                @*<input type="checkbox" id="cbx" style="display: none;">*@
                @*<label for="cbx" class="check">
                <svg width="18px" height="18px" viewBox="0 0 18 18">
                    <path d="M1,9 L1,3.5 C1,2 2,1 3.5,1 L14.5,1 C16,1 17,2 17,3.5 L17,14.5 C17,16 16,17 14.5,17 L3.5,17 C2,17 1,16 1,14.5 L1,9 Z"></path>
                    <polyline points="1 9 7 14 15 4"></polyline>
                </svg>
            </label>*@
                <p>Remember me</p>

            </form>
            <div class="btn">
                <!--                    <button>FORGOT PASSWORD</button>-->
                <button>LOG IN</button>
            </div>
            @if (TempData["loginmsg"] != null)
            {
                <p style="margin-left: 10px; position:relative; top: -30px; left: 150px; color: #e67e22;">Username or Password not match</p>
             }
            <div class="privacy">
                    <span>©2019 Arche Softronix Privacy and Terms</span>
                </div>
            </div>
    </div>
</div>

}